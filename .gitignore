__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
# Local eval media and model outputs — keep the repo about code and gold specs.
# Video files are ignored by extension so the data/videos README stays tracked.
*.mp4
*.mov
out/
